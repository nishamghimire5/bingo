*******************************************************************************

This application uses GTK to design the graphical user interface.

It will not compile in a system that doesn't have GTK installed, specifically, GTK4.

One may wish to follow the instructions on gtk.org to install GTK and compile the program in their local machine.

To compile, one need only run the 'make' command and run the application with ./bingo.
